const registerDirector = document.getElementById('RegisterDirector');
registerDirector.onclick = () => window.location.replace('/pages/registerDirector.html');

const editDirector = document.getElementById('EditDirector');
editDirector.onclick = () => window.location.replace('/pages/editDirector.html');
